# Local Postgres (docker compose) or Supabase
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/wavefront?schema=public"

# JWT for demo auth (change in production)
JWT_SECRET="wavefront-dev-secret-change-me"

PORT=4000
